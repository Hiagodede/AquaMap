<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             xmlns:vm="clr-namespace:AquaMap.ViewModels"
             xmlns:models="clr-namespace:AquaMap.Domain.Entities;assembly=AquaMap.Domain"
             x:Class="AquaMap.MainPage"
             x:DataType="vm:MainViewModel"
             Title="Pontos de Coleta">

    <Grid RowDefinitions="Auto, *">

        <Label Text="Monitoramento AquaMap" 
               FontSize="24" 
               FontAttributes="Bold"
               HorizontalOptions="Center" 
               Margin="0,20,0,10"/>

        <CollectionView Grid.Row="1" 
                        ItemsSource="{Binding Points}"
                        Margin="20">
            <CollectionView.ItemTemplate>
                <DataTemplate x:DataType="models:Reservoir">
                    <Border Margin="0,5" 
                            Padding="10" 
                            Stroke="Gray" 
                            StrokeShape="RoundRectangle 10">

                        <VerticalStackLayout>
                            <Label Text="{Binding Name}" 
                                   FontSize="18" 
                                   FontAttributes="Bold" />
                        </VerticalStackLayout>

                    </Border>
                </DataTemplate>
            </CollectionView.ItemTemplate>
        </CollectionView>

    </Grid>

</ContentPage>